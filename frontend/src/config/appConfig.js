// src/config/appConfig.js
// 全局业务配置

export default {
  businessConfig: {
    capsule: {
      visibilityOptions: [
        { key: 'public', label: '校园公开' },
        { key: 'friend', label: '好友可见' },
        { key: 'private', label: '仅自己可见' }
      ]
    }
  }
}
